{
  "name": "ff14msq",
  "version": "1.0.0",
  "description": "FFXIV Main Scenario Quest Summarizer with caching",
  "main": "index.html",
  "scripts": {
    "dev": "vite",
    "dev-full": "concurrently \"npm run server\" \"npm run dev\"",
    "build": "vite build",
    "preview": "vite preview",
    "server": "node server.js",
    "start": "node server.js",
    "lint": "eslint src --ext .js",
    "lint:fix": "eslint src --ext .js --fix"
  },
  "devDependencies": {
    "concurrently": "^8.0.0",
    "eslint": "^8.0.0",
    "vite": "^5.0.0"
  },
  "type": "module",
  "dependencies": {
    "express": "^5.1.0",
    "level": "^10.0.0"
  }
}
